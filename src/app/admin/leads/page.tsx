import { redirect } from 'next/navigation'; import Link from 'next/link'; import { isAdminLoggedIn } from '@/lib/admin-auth'; import LeadsClient from './LeadsClient';
export default async function LeadsPage() { if (!(await isAdminLoggedIn())) redirect('/admin/login'); return <main style={{ minHeight: '100vh', background: '#fdfbf0', padding: '2rem 1rem' }}><div className="container" style={{ maxWidth: 1100 }}><Link href="/admin" style={{ color: '#8a6025', textDecoration: 'none' }}>← Dashboard</Link><h1 style={{ margin: '1.5rem 0 .35rem', color: '#0a0806' }}>Pipeline Hajj & Omra</h1><p style={{ color: '#78716c', marginBottom: '1.5rem' }}>Qualification, attribution, suivi et commissions.</p><LeadsClient /></div></main>; }
