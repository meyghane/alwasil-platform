import { redirect } from 'next/navigation'; import Link from 'next/link'; import { isAdminLoggedIn } from '@/lib/admin-auth'; import FreshnessClient from './FreshnessClient';
export default async function FreshnessPage() { if (!(await isAdminLoggedIn())) redirect('/admin/login'); return <main style={{ minHeight:'100vh', background:'#f0ebfa', padding:'2rem 1rem' }}><div className="container" style={{ maxWidth:900 }}><Link href="/admin" style={{color:'#543398',textDecoration:'none'}}>← Dashboard</Link><h1 style={{margin:'1.5rem 0 .35rem'}}>Fraîcheur des fiches</h1><p style={{color:'#59565f'}}>Fiches arrivées à leur date de vérification.</p><FreshnessClient/></div></main>; }
