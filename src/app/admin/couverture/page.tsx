import { redirect } from 'next/navigation'; import Link from 'next/link'; import { isAdminLoggedIn } from '@/lib/admin-auth'; import CoverageClient from './CoverageClient';
export default async function CoveragePage(){if(!(await isAdminLoggedIn()))redirect('/admin/login');return <main style={{minHeight:'100vh',background:'#f0ebfa',padding:'2rem 1rem'}}><div className="container" style={{maxWidth:1000}}><Link href="/admin" style={{color:'#543398',textDecoration:'none'}}>← Dashboard</Link><h1 style={{margin:'1.5rem 0 .35rem'}}>Couverture géographique</h1><p style={{color:'#59565f'}}>Priorité aux 8 départements d’Île-de-France. Objectif initial : 3 fiches fiables par catégorie.</p><CoverageClient/></div></main>}
